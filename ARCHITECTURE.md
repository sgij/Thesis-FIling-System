# 🎨 Visual Project Architecture

## 📊 File Organization Diagram

```
html_template/
│
├── 📂 src/                          ← YOUR WORKSPACE (Edit here!)
│   │
│   ├── 📄 index.html               ← Main HTML file
│   │
│   ├── 📂 css/                     ← Stylesheets
│   │   └── 📄 style.css           ← All CSS styles
│   │
│   └── 📂 js/                      ← JavaScript
│       └── 📄 main.js              ← Application logic
│
├── 📂 public/                       ← Static assets (images, fonts)
│   └── 📂 assets/
│       └── 📂 images/
│
├── 📂 dist/                         ← BUILD OUTPUT (Auto-generated)
│   ├── 📄 index.html
│   └── 📂 assets/
│       ├── 📂 css/
│       ├── 📂 js/
│       └── 📂 images/
│
├── 📂 .vscode/                      ← Editor settings
│   ├── 📄 settings.json
│   └── 📄 extensions.json
│
├── 📂 node_modules/                 ← Dependencies (Auto-generated)
│
├── 📄 .editorconfig                ← Editor consistency
├── 📄 .env.example                 ← Environment template
├── 📄 .eslintrc.json              ← Code linting rules
├── 📄 .gitignore                   ← Git ignore list
├── 📄 .prettierrc                  ← Code formatting
├── 📄 .prettierignore             ← Format ignore list
│
├── 📄 CHANGELOG.md                 ← Version history
├── 📄 CONTRIBUTING.md              ← How to contribute
├── 📄 DEPLOYMENT.md                ← Deploy guides
├── 📄 IMPROVEMENTS.md              ← What's new
├── 📄 LICENSE                      ← MIT License
├── 📄 QUICKSTART.md                ← Fast start guide
├── 📄 README.md                    ← Main documentation
├── 📄 STRUCTURE.md                 ← This structure
│
├── 📄 jsconfig.json                ← JavaScript config
├── 📄 package.json                 ← Project metadata
├── 📄 pnpm-lock.yaml              ← Dependency lock
├── 📄 template_config.json        ← Template settings
└── 📄 vite.config.js              ← Build configuration
```

---

## 🔄 Development Workflow

```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│  Developer edits files in src/                         │
│  ├── src/index.html                                    │
│  ├── src/css/style.css                                 │
│  └── src/js/main.js                                    │
│                                                         │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│  DEVELOPMENT MODE (pnpm dev)                           │
│                                                         │
│  Vite Dev Server                                       │
│  ├── Hot Module Replacement (HMR)                     │
│  ├── Instant updates on save                          │
│  ├── Source maps for debugging                        │
│  └── Serves at http://localhost:5173                  │
│                                                         │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│  BROWSER                                               │
│  ├── View changes instantly                           │
│  ├── Debug with DevTools                              │
│  └── Test features                                     │
└─────────────────────────────────────────────────────────┘
```

---

## 🏗️ Build Process

```
┌─────────────────────────────────────────────────────────┐
│  SOURCE FILES (src/)                                   │
│  ├── index.html                                        │
│  ├── css/style.css                                     │
│  └── js/main.js                                        │
└────────────────┬────────────────────────────────────────┘
                 │
                 │ pnpm build
                 ▼
┌─────────────────────────────────────────────────────────┐
│  VITE BUILD PROCESS                                    │
│  ├── Bundle JavaScript                                │
│  ├── Minify CSS                                        │
│  ├── Optimize HTML                                     │
│  ├── Compress assets                                   │
│  ├── Generate source maps                             │
│  ├── Hash filenames                                    │
│  └── Tree-shake unused code                           │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│  PRODUCTION BUILD (dist/)                              │
│  ├── index.html (optimized)                           │
│  └── assets/                                           │
│      ├── css/style-[hash].css (minified)             │
│      ├── js/main-[hash].js (minified)                │
│      └── images/ (optimized)                          │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│  DEPLOYMENT                                            │
│  ├── Netlify                                           │
│  ├── Vercel                                            │
│  ├── GitHub Pages                                      │
│  └── Traditional Hosting                               │
└─────────────────────────────────────────────────────────┘
```

---

## 🔍 Code Quality Pipeline

```
┌─────────────────────────────────────────────────────────┐
│  DEVELOPER WRITES CODE                                 │
│  └── Edits in src/                                     │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│  EDITORCONFIG                                          │
│  └── Enforces: tabs/spaces, line endings, etc.        │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│  PRETTIER (Auto-format on save)                        │
│  ├── Consistent formatting                            │
│  ├── Line length                                       │
│  └── Quote styles                                      │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│  ESLINT (Code quality)                                 │
│  ├── Catch errors                                      │
│  ├── Best practices                                    │
│  ├── Unused variables                                  │
│  └── Code smells                                       │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│  CLEAN, QUALITY CODE ✨                                │
└─────────────────────────────────────────────────────────┘
```

---

## 📦 Dependency Tree

```
html_template
│
├── Dependencies (node_modules/)
│   │
│   ├── vite
│   │   └── Fast dev server & bundler
│   │
│   ├── eslint
│   │   └── Code linting
│   │
│   └── prettier
│       └── Code formatting
│
└── Source Code (src/)
    │
    ├── HTML
    │   └── Structure
    │
    ├── CSS
    │   └── Styling
    │
    └── JavaScript
        └── Functionality
```

---

## 🎯 File Relationships

```
┌──────────────────┐
│   index.html     │
│                  │
│  Links to:       │
│  ├── style.css  │ ────────┐
│  └── main.js    │ ───┐    │
└──────────────────┘    │    │
                        │    │
                        ▼    ▼
              ┌──────────────────┐
              │   CSS Variables  │
              │   ├── Colors     │
              │   ├── Fonts      │
              │   └── Spacing    │
              └──────────────────┘
                        │
                        ▼
              ┌──────────────────┐
              │  JavaScript      │
              │  ├── Classes     │
              │  ├── Functions   │
              │  └── Event       │
              │     Handlers     │
              └──────────────────┘
                        │
                        ▼
              ┌──────────────────┐
              │  LocalStorage    │
              │  ├── Files       │
              │  ├── Settings    │
              │  └── State       │
              └──────────────────┘
```

---

## 🛠️ Tool Integration

```
┌─────────────────────────────────────────────────────────┐
│                    VS Code Editor                       │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  Extensions:                                           │
│  ├── ESLint        → Real-time error checking         │
│  ├── Prettier      → Auto-format on save              │
│  ├── EditorConfig  → Consistent settings              │
│  └── Path Intel.   → Smart path completion            │
│                                                         │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  Settings (.vscode/settings.json):                    │
│  ├── formatOnSave: true                               │
│  ├── defaultFormatter: prettier                        │
│  └── codeActionsOnSave: eslint fix                    │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 🚀 Command Flow

```
Developer Commands:
├── pnpm dev        → Start dev server → localhost:5173
├── pnpm build      → Build production → dist/
├── pnpm preview    → Preview build    → localhost:4173
├── pnpm lint       → Check code       → Report errors
├── pnpm lint:fix   → Fix errors       → Auto-fix issues
└── pnpm format     → Format code      → Prettier format
```

---

## 🎨 Theme System

```
┌─────────────────────────────────────────────────────────┐
│                   CSS Variables                         │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  :root (Dark Theme - Default)                         │
│  ├── --bg-primary: #0a0e1a                            │
│  ├── --text-primary: #ffffff                          │
│  └── --accent-primary: #3b82f6                        │
│                                                         │
│  [data-theme="light"]                                 │
│  ├── --bg-primary: #ffffff                            │
│  ├── --text-primary: #1e293b                          │
│  └── --accent-primary: #3b82f6                        │
│                                                         │
└─────────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────┐
│  JavaScript toggles data-theme attribute               │
│  → Automatic theme switching                           │
└─────────────────────────────────────────────────────────┘
```

---

## 📱 Responsive Design

```
Desktop (1200px+)      Tablet (768px-1199px)    Mobile (< 768px)
┌─────────────────┐    ┌──────────────┐         ┌─────────┐
│ [Nav          ]│    │ [Nav       ]│         │ [Nav  ]│
│ ├───┬─────────┤│    │ ├─────────┤│         │ ├─────┤│
│ │   │         ││    │ │         ││         │ │     ││
│ │ S │ Content ││    │ │ Content ││         │ │     ││
│ │ i │         ││    │ │         ││         │ │ C   ││
│ │ d │         ││    │ │         ││         │ │ o   ││
│ │ e │         ││    │ │         ││         │ │ n   ││
│ │   │         ││    │ │         ││         │ │ t   ││
│ │   │         ││    │ └─────────┘│         │ │ e   ││
│ └───┴─────────┘│    └──────────────┘         │ │ n   ││
└─────────────────┘                            │ │ t   ││
                                               │ └─────┘│
Full sidebar        Collapsible sidebar       Mobile view
```

---

**Legend:**
- 📂 Directory
- 📄 File
- ← Important
- ✨ Optimized

This visual guide helps you understand how everything fits together!
